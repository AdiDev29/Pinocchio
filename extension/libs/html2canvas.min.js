/* This would be the minified html2canvas library, but I'm replacing it with a simpler approach that doesn't rely on html2canvas */
console.log("PDF functionality replaced with direct HTML download");
